// Placeholder smoke test.
//
// `flutter create` regenerates this file on every Init run with a default
// MyApp reference that doesn't match our real app class (SelectoApp). To
// avoid having the test directory break on every fresh setup, we keep a
// stub here that always passes and depends on nothing app-specific.
//
// Real tests live in test/domain/ and friends.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test directory compiles', () {
    expect(true, isTrue);
  });
}
